{
  "name": "chat_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev-ui": "npm run dev --prefix packages/client",
    "dev-backend": "npm run dev --prefix packages/server",
    "build-ui": "npm run build --prefix client",
    "build-backend": "npm run build --prefix backend",
    "dev":"concurrently npm:dev-*",
    "build": "concurrently npm:build-*",
    "start": "npm run start --prefix backend"
  },
  "workspaces": [
    "packages/**"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
